# Copy to terraform.tfvars (gitignored) and fill in.
# hcloud_token is read from the env var TF_VAR_hcloud_token instead.
ssh_public_key = "ssh-ed25519 AAAA... you@host"
server_type    = "cax11"
location       = "nbg1"
